package components

// Maintenu pour la compatibilité avec le code existant, utilise le nouveau composant Toast en interne
templ ErrorBanner(message string) {
    @Toast(ToastLevelError, "Error", message)
}